# FreeCAD backups — regenerated on every save
*.FCBak

# Python
__pycache__/
*.py[cod]

# Render output — derived from render/*.py, regenerate with `python3 render.py`
render/*.svg
render/*.png

# KiCad — autosave, backups and derived output
*-backups/
*.kicad_prl
_autosave-*
fp-info-cache
*.kicad_sch-bak
*.kicad_pcb-bak
